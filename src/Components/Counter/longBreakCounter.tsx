import React from "react";
import { Box } from "theme-ui";

const LongBreakCounter = () => {
  return (
    <Box
      sx={{
        fontSize: 120,
        fontWeight: "bold",
        marginTop: 20,
        fontFamily: "ArialRoundedMTBold",
        color: "white",
      }}
    >
      15:00
    </Box>
  );
};

export default LongBreakCounter;
